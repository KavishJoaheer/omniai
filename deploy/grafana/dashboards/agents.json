{
  "__inputs": [{"name": "DS_PROMETHEUS", "label": "Prometheus", "description": "", "type": "datasource", "pluginId": "prometheus"}],
  "title": "Omni-AI — Agents & Chat",
  "uid": "omniai-agents",
  "schemaVersion": 38,
  "version": 1,
  "refresh": "1m",
  "time": {"from": "now-6h", "to": "now"},
  "panels": [
    {
      "id": 1, "type": "stat", "title": "Chat Messages (last hour)", "gridPos": {"x":0,"y":0,"w":6,"h":4},
      "targets": [{"expr": "sum(increase(omniai_chat_messages_total{status='success'}[1h]))", "datasource": {"type":"prometheus","uid":"${DS_PROMETHEUS}"}}]
    },
    {
      "id": 2, "type": "stat", "title": "Chat Error Rate", "gridPos": {"x":6,"y":0,"w":6,"h":4},
      "targets": [{"expr": "sum(rate(omniai_chat_messages_total{status='error'}[10m])) / (sum(rate(omniai_chat_messages_total[10m])) + 0.001)", "datasource": {"type":"prometheus","uid":"${DS_PROMETHEUS}"}}],
      "fieldConfig": {"defaults": {"unit": "percentunit"}},
      "options": {"thresholds": {"steps": [{"value":0,"color":"green"},{"value":0.05,"color":"yellow"},{"value":0.15,"color":"red"}]}}
    },
    {
      "id": 3, "type": "timeseries", "title": "Chat Messages by Status", "gridPos": {"x":0,"y":4,"w":24,"h":8},
      "targets": [{"expr": "sum(rate(omniai_chat_messages_total[2m])) by (status)", "legendFormat": "{{status}}", "datasource": {"type":"prometheus","uid":"${DS_PROMETHEUS}"}}],
      "fieldConfig": {"defaults": {"unit": "reqps"}}
    }
  ]
}
